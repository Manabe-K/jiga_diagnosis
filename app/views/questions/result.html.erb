<div class="min-h-screen flex flex-col justify-center items-center text-center p-6 sm:p-10 bg-gray-50">
  <h2 class="text-3xl sm:text-4xl font-bold mb-6 text-gray-800 tracking-wide">診断結果</h2>

  <% if @character.present? %>
    <p class="text-lg sm:text-xl max-w-md text-gray-700 leading-relaxed mb-4">
      あなたの自我レベルは…
    </p>

    <div class="text-4xl sm:text-5xl font-extrabold text-blue-600 mb-2 animate-pulse">
      Lv.<%= @character[:level] %>：<%= @character[:name] %>
    </div>

    <p class="text-md sm:text-lg text-gray-600 max-w-xl leading-relaxed">
      <%= @character[:description] %>
    </p>
  <% else %>
    <p class="text-gray-500">診断に失敗しました。</p>
  <% end %>

  <div class="mt-8">
    <%= link_to "もう一度診断する", root_path, class: "inline-block bg-blue-500 hover:bg-blue-600 text-white px-6 py-2 rounded-lg font-semibold transition" %>
  </div>
</div>